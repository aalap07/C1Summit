import React from 'react';
import './Header.css';

const Header = (props) => {
    return (
        <div className="Header">
            <header className="App_header">
                
            </header>
        </div>
    );
}

export default Header;